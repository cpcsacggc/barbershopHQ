<h2>Записаться</h2>
<p>На этой странице вы сможете записаться к нам в Barber Shop</p>


<script type="text/javascript">
$(function() {

  // тут я могу написать любой javascript код, который исполнится
  // после загрузки страницы

  $('#color').simplecolorpicker();
  $('#datetimepicker').datetimepicker();
  
});


</script>


<form action="/visit" method="POST" role="form">

  <div class="form-group">
    <label>Ваше имя</label>
    <input name="user[name]" type="text" class="form-control" placeholder="Ваше имя" value="<%= @u.name %>">
  </div>

  <div class="form-group">
    <label>Ваш телефон</label>
    <input name="user[phone]" type="text" class="form-control" placeholder="Ваш телефон" value="<%= @u.phone %>">
  </div>

  <div  class="form-group">
    <label>Когда вас ждать?</label>
    <input id="datetimepicker" name="user[datestamp]" type="text" class="form-control" placeholder="Введите дату и время" value="<%= @u.datestamp %>"> 
  </div>


  <div class="form-group">
    <label>Ваш парикмахер:</label>
    <select id="barber" name="user[barber]" class="form-control">
      <option></option>
      <% @barbers.each do |barber| %><option <%= @u.barber == barber.name ? 'selected' : '' %>><%= barber.name %></option>
      <% end %>
    </select>
  </div>

  <div class="form-group">
    <label>В какие цвета хотите себя окрасить: </label>

    <select id="color" name="user[color]" class="form-control">
      <option value=""></option>
      <% @colors.each do |color| %><option value="<%=color.value %>" <%= @u.color == color.value ? 'selected' : '' %>><%= color.name %></option>
      <% end %>   
    </select>
  </div>

  <button type="submit" class="btn btn-primary">Записаться</button>

</form>
